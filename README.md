# XDeck

X（Twitter）の検索結果を複数カラムで横並び閲覧するための、個人利用向けデスクトップアプリ（Electron製）。

## セットアップ

```bash
npm install
npm start
```

初回起動時、Windowsから「パブリックネットワークとプライベートネットワークへのアクセスを許可しますか」という
ファイアウォールの確認が出ます。**許可**を選んでください（X.comへの通信に必要です）。

## 使い方

1. 上部のフォームでカラム種別を選ぶ。
   - 「検索」: 検索キーワードとソート順（最新/話題）を指定して「＋カラム追加」。キーワード欄の右にある
     チェックボックス（日本語/メディア/RT除外/返信除外）で検索演算子（`lang:ja` / `filter:media` /
     `-filter:retweets` / `-filter:replies`）を`q=`に注入できます。既定はすべてOFFで、カラムごとに
     選んだ組み合わせが`columns.json`に保存されます（追加後の演算子変更は非対応。変更したい場合は
     カラムを削除して追加し直してください）。
   - 「ホーム（フォロー中）」: `x.com/home` を表示するカラム。「おすすめ/フォロー中」タブは
     アプリ側で自動的に「フォロー中」を選択したうえで非表示にしているので、常にフォロー中の
     タイムラインだけが表示されます。投稿ボックス（「いまどうしてる？」）も非表示にし、閲覧スペースを
     最大化しています（投稿は下記の「✎ ポストする」から行えます）。
   - 「トレンド」: X全体のトレンド一覧（`x.com/explore/tabs/trending`）。
   - 「ホーム」「トレンド」ではキーワード入力欄は不要になり自動的に隠れます。
2. 追加したカラムは自動的に `%APPDATA%\xdeck\columns.json` に保存され、次回起動時も復元されます。
3. **ログイン**は特別な仕組みを用意していません。いずれか1つのカラムでXのログイン画面が出るので、そこで直接ログインしてください。
   全カラムが同じセッション（`persist:xsession` パーティション）を共有しているため、一度ログインすれば
   他のカラムにも、次回起動時にも引き継がれます。
4. 各カラムのヘッダーにあるボタン：
   - `⠿` ドラッグして並べ替え（他のカラムの上にドロップすると入れ替わります）
   - `⌂` トップ（検索/ホーム/トレンドそれぞれの元のURL）に戻る
   - `←` ブラウザの「戻る」
   - `⟳` 手動更新
   - `⚙` そのカラムの「文字サイズ」「自動更新」の設定を開閉
   - `🔍` DevTools（下記のCSS調整で使用）
   - `✕` カラム削除

   カラムの右端（境界線）をドラッグすると幅を自由に変更できます（280px〜900px、`columns.json`に永続化）。
5. 文字サイズ（80%〜150%）と自動更新間隔（オフ/1分/3分/5分/10分、既定は3分）は**カラムごと**に`⚙`から設定できます
   （既定では非表示。`⚙`をクリックした時だけ開くパネルです）。
   文字サイズはCSSの`zoom`プロパティを各カラムの`<webview>`に個別注入して実現しています
   （`webview.setZoomFactor()`はChromiumの仕様上、同じセッション・同じオリジンの全カラムに影響してしまうため
   採用していません）。設定はカラムごとに`columns.json`へ保存されます。
   自動更新はフルリロードではなく、X自身が表示する「新しいポストがあります」ボタン（「.」キー相当）をJS経由で
   クリックする方式です。新着があればページ遷移なしでその場に反映され、新着が無ければ何も起きません。
   手動更新（`⟳`）は従来通りの完全リロードのままです。
6. 投稿は右上の「✎ ポストする」ボタンから。同じセッション（`persist:xsession`）を使う小さなポップアップ
   ウィンドウで`x.com/compose/post`（Xの投稿専用画面）を開きます。各カラムはあくまで閲覧専用（投稿ボックス等は
   非表示）にして、投稿の導線はこのボタン1つに集約する設計にしています。
7. タイムライン上の広告（プロモーション投稿）は全カラムで自動的に非表示になります。無限スクロールで後から
   読み込まれる投稿にも追従するよう、`MutationObserver`で継続的に監視・非表示化しています（`renderer.js`の
   `POST_FILTER_JS`）。広告はXの内部実装的に`data-testid`等の安定した目印を持たない素の`<span>`で「広告」と
   表示されるだけなので、テキスト内容で判定しています（ツイート本文自体は判定対象から除外し、本文にたまたま
   「広告」という言葉が含まれていても誤って隠さないようにしています）。ミュートワード機能を将来追加する際も、
   同じ仕組みの`filters`配列に条件を1つ足すだけで対応できる設計にしてあります。
8. ツイート内のリンクや外部サイトのカードをクリックすると、アプリ内には表示せず**OSの既定のブラウザ**で開きます。
   x.com/twitter.com以外のURLへのアクセスはすべて`shell.openExternal()`に回し、Electron側では新規ウィンドウも
   作らないようにしています（`main.js`の`interceptExternalNavigation`、`webContents.setWindowOpenHandler` +
   `will-navigate`）。x.com内のリンク（プロフィール、ハッシュタグなど）はそのままカラム内で遷移します。

## 不要UIの非表示（CSS注入）について

[`inject.css`](inject.css) がタイムライン以外の要素（左ナビ、右サイドバーのトレンド/おすすめ、投稿ボタン、
**Xが自前で表示する検索ボックスや「話題のポスト/最新」等のタブバー**、ホーム画面の「タイムラインを管理」
ボタンとその余白など）を `display: none` で隠しています。子要素だけを隠すとその親の空き枠（余白）が
透けて残ってしまうことがあるため、`:has()`で「対象を含み、かつタイムライン本体（`article`）は含まない」
親要素ごと隠すようにしています。
検索・ソート切り替えはアプリ側のツールバー（キーワード入力・ソートのプルダウン・カラム種別選択）で行う設計なので、
各カラムは結果だけを表示する「出力用」の画面になります。X（Twitter）はDOM構造やクラス名を頻繁に変更するため、
**このファイルは壊れる前提**で設計してあります。

- アプリを起動したまま `inject.css` を編集して保存すると、開いている全カラムに即座に再適用されます（再起動不要）。
- 不要な要素が復活してきたら、カラムの `🔍` ボタンでDevToolsを開き、該当要素を検証（Inspect）して
  `data-testid` 属性などの安定したセレクタを探し、`inject.css` に追記してください。

## exe化（配布用パッケージの作成）

```bash
npm run package:win
```

`node_modules\electron\dist`（`npm install`済みのElectronランタイム）をそのまま流用し、`resources\app`に
アプリのソースを配置する形で `dist\XDeck-win32-x64\XDeck.exe` を組み立てます（内部は
[`build-package.ps1`](build-package.ps1)）。`electron-builder`/`electron-packager`のようなインストーラー生成は
行っていません（この環境ではzip展開処理が不安定だったため）。生成される`dist\XDeck-win32-x64`フォルダ一式
（約260MB、Chromium同梱のため）をまるごとコピー/zipすれば、Node.js未インストールの環境でも`XDeck.exe`を
ダブルクリックするだけで起動できます。

コード変更後の再ビルドは同じコマンドで、既存の`XDeck.exe`（Electron本体）はそのまま再利用し、
`resources\app`配下のソースだけ上書きされます。Electronのバージョン自体を上げた場合は
`dist\XDeck-win32-x64`フォルダを削除してから実行してください。

## 技術構成

- `main.js` — Electronメインプロセス。カラム構成の読み書き（JSON）、`inject.css`のファイル監視、外部リンクを
  既定ブラウザで開く処理を担当。
- `preload.js` — `contextBridge` でレンダラーに `window.xdeck` として最小限のAPIを公開（`contextIsolation: true`）。
- `index.html` / `styles.css` — アプリ全体のシェル（ツールバー、カラムを横並びにするコンテナ）。
- `renderer.js` — カラムの追加・削除、`<webview>` の生成、CSS再注入、自動更新タイマーを制御。
- `inject.css` — 各カラム（＝各 `<webview>` が表示するx.comのページ）に注入するCSS。

各カラムは `<webview partition="persist:xsession">` として実装しており、これが「複数アカウント不要・単一セッション
共有・ディスクへの永続化」を同時に満たしています。

## 既知の制約（MVPとして割り切っている点）

- 自動更新は `webview.reload()` によるフルリロードです（スクロール位置は保持されません）。よりスムーズな
  差分更新にするなら、X内蔵の「新着ツイート」ピルをJS経由でクリックする方式への改善余地があります。
- `inject.css` のセレクタはX側の変更で壊れる可能性があります（上記「不要UIの非表示について」参照）。
