# XDeck 実装候補リスト

現状はMVP + トレンドカラム + 結果専用表示(検索ボックス/タブ非表示)まで実装済み。
以下は仕様書の意図や運用上あると嬉しい機能の洗い出し(優先度は未整理、後で相談して絞り込む想定)。

## 表示設定
- [x] 文字サイズ設定(80%〜150%、`<webview>`のズーム機能で実現。`settings.json`に永続化)

## カラム操作まわり
- [x] カラムの並べ替え(ドラッグ&ドロップ)。ヘッダーの`⠿`ハンドルをドラッグして入れ替え可能
- [ ] カラム幅の個別リサイズ(現状は固定380px)
- [ ] カラムごとの自動更新間隔の個別設定(現状はアプリ全体で一括)
- [ ] 自動更新をフルリロードでなく差分更新に(X内蔵の「新着」ピルをJS経由でクリックし、スクロール位置を保持)
- [ ] カラムごとのミュートワード(特定キーワードを含む投稿を非表示)

## トレンド・検索の拡張
- [ ] トレンドの地域指定(現状はX標準の「おすすめ」トレンドのみ。国・地域別に切り替えたい場合はWOEID等の調査が必要)
- [ ] 検索演算子をアプリ側UIから組み立てられるように(`from:`, `filter:media`, `since:`/`until:` など。現状はキーワードをそのまま`q=`に渡すだけ)

## 常駐・ウィンドウまわり(「仮想デスクトップに常駐」という当初要件向け)
- [ ] 閉じるボタンでタスクトレイに格納(×で終了せず常駐)
- [ ] ウィンドウの位置・サイズを記憶して次回起動時に復元
- [ ] Windows起動時の自動起動(スタートアップ登録)

## 保守性
- [ ] `inject.css`のセレクタ切れを検知する簡易ヘルスチェック(定期的に想定要素の有無をログ出力するなど)
- [ ] ネットワーク切断/レート制限時にカラム内へエラー表示

## 配布まわり
- [ ] インストーラー化の検討(現状はポータブルフォルダ形式。`electron-builder`でNSISインストーラーを試すか要検討)
- [ ] 未署名exeによるWindows SmartScreen警告への対応方針(コード署名するか、都度「詳細情報→実行」で回避する運用にするか)

## 対象外にした機能(仕様書上、意図的に実装しない)
- 投稿・リプライ・複数アカウント切り替えなどの発信系機能
