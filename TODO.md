# XDeck 実装候補リスト

現状はMVP + トレンドカラム + 結果専用表示(検索ボックス/タブ非表示)まで実装済み。
以下は仕様書の意図や運用上あると嬉しい機能の洗い出し(優先度は未整理、後で相談して絞り込む想定)。

## 表示設定
- [x] 文字サイズ設定(80%〜150%、カラムごとに独立。`<webview>`のCSS注入で実現し、`columns.json`に永続化)
- [x] カラム設定(⚙)パネルは既定で非表示、クリックで開閉(実装直後は`hidden`属性がCSSに上書きされて常時表示になるバグがあったため修正済み)
- [x] ツールバーの`🔍`DevToolsボタンを廃止(常設アイコンは不要と判断。保守用にF12キーで該当カラムのDevToolsを開けるようにして代替)

## カラム操作まわり
- [x] カラムの並べ替え(ドラッグ&ドロップ)。ヘッダーの`⠿`ハンドルをドラッグして入れ替え可能
- [x] カラムごとの自動更新間隔の個別設定(`⚙`から設定、`columns.json`に永続化)
- [x] ホームタイムラインのカラム(`x.com/home`。「おすすめ/フォロー中」タブは非表示、投稿ボックスも非表示にして閲覧スペースを最大化)
- [x] タブ非表示に伴い見えなくなった「今どの状態か」を`⚙`で可視化・切り替え可能に(検索の並び替え[最新/話題]、ホームの表示[おすすめ/フォロー中]。カラムタイトルにも現在の状態を表示。ホームの切り替えはページ遷移なしのタブクリックで実現)
- [x] 通知タイムラインのカラム(`x.com/notifications`。「すべて/@ツイート」タブは非表示、既定で「すべて」が選択済みなので支障なし。ホームで対応した空き枠の非表示ルールがそのまま効くことを確認済み)
- [x] 投稿ボタン(「✎ ポストする」で`x.com/compose/post`を別ポップアップウィンドウで開く。各カラムは閲覧専用に統一)
- [x] 投稿成功時にポップアップを自動で閉じる(`CreateTweet` GraphQL通信の成功[2xx]を`webRequest.onCompleted`で検知。実投稿で検知自体は成功していたが`close()`が効かない事例があり[Xの`beforeunload`が阻んでいる可能性]、`destroy()`に変更。実際の通信ログを`compose-debug.log`に記録する仕組みも追加)
- [x] タイムライン上の広告(プロモーション投稿)を非表示(全カラム共通。`POST_FILTER_JS`のMutationObserverで無限スクロールにも追従)
- [x] ホーム上部の「タイムラインを管理」ボタンとその余白を非表示
- [x] 外部リンク・カードのクリックはOSの既定ブラウザで開く(アプリ内に新規ウィンドウを作らない。`<webview>`の`new-window`イベントは非推奨かつpreventDefault()が効かなかったため、`webContents.setWindowOpenHandler`+`will-navigate`に切り替え)
- [x] カラム幅の個別リサイズ(280px〜900px。カラム右端のハンドルをドラッグ。`<webview>`は独立した描画レイヤーでハンドルを重ねると入力を奪われるため、専用の非重複ストリップ+ドラッグ中は全webviewの`pointer-events:none`で対応)
- [x] 自動更新をフルリロードでなく差分更新に(X自身の「新しいポストがあります」ボタンをJS経由でクリック。新着が無ければ何もしない。手動更新`⟳`は従来通り完全リロードのまま)
- [ ] カラムごとのミュートワード(特定キーワードを含む投稿を非表示。広告フィルタと同じ`POST_FILTER_JS`の`filters`配列に条件を1つ追加するだけで実装できる設計にしてある)

## トレンド・検索の拡張
- [ ] トレンドの地域指定(現状はX標準の「おすすめ」トレンドのみ。国・地域別に切り替えたい場合はWOEID等の調査が必要)
- [x] 検索演算子のチェックボックス(`lang:ja` / `filter:media` / `-filter:retweets` / `-filter:replies`。数値・日付入力が要る`min_faves:`/`since:`/`until:`等はチェックボックスに不向きなため対象外。既定は全てOFF。追加後も`⚙`で現在の状態を確認・変更可能、変更すると即座に再検索)
- [x] ハッシュタグクリック時、そのカラムを検索結果で上書きせず、右隣りに新規検索カラムとして挿入(一番右に追加すると元カラムとの対応が分かりにくいため。`webview-preload.js`でハッシュタグリンクの遷移を捕捉し`xdeck-hashtag-click`で通知、`renderer.js`の`insertColumnAfter`が挿入を担当)

## 常駐・ウィンドウまわり(「仮想デスクトップに常駐」という当初要件向け)
- [ ] 閉じるボタンでタスクトレイに格納(×で終了せず常駐)
- [x] ウィンドウの位置・サイズを記憶して次回起動時に復元(最大化状態も含む。`window-state.json`。保存位置が画面外になる場合はサイズのみ引き継いで位置は既定に戻す安全策付き)

## 保守性
- [ ] `inject.css`のセレクタ切れを検知する簡易ヘルスチェック(定期的に想定要素の有無をログ出力するなど)
- [ ] ネットワーク切断/レート制限時にカラム内へエラー表示

## 配布まわり
- [ ] インストーラー化の検討(現状はポータブルフォルダ形式。`electron-builder`でNSISインストーラーを試すか要検討)
- [ ] 未署名exeによるWindows SmartScreen警告への対応方針(コード署名するか、都度「詳細情報→実行」で回避する運用にするか)

## 対象外にした機能(意図的に実装しない、または後回し)
- 複数アカウント切り替え
- 各カラム内でのリプライ・いいね等の個別操作(投稿のみ「✎ ポストする」ボタンで対応)
- Windows起動時の自動起動(スタートアップ登録)
